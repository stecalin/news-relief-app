// entry point of the app - for now, it just renders the feed screen
// as the app grows, this is where we'd add navigation between screens

import FeedScreen from './src/screens/FeedScreen';

export default function App() {
  return <FeedScreen />;
}